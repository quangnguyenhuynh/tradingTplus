# CLI tests

Tests for production command routing, exit codes, normalized symbol arguments, dry-run defaults, explicit write flags, and safe script imports.

```bash
python -m pytest -q tests/cli
```
