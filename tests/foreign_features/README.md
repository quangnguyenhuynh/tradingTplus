# Foreign feature tests

Offline tests cover Decimal formulas, 5/20 windows, disjoint change windows,
invalid values, duplicate/missing sessions, read-only services and migration
contracts. PostgreSQL integration must run only against an isolated local/test DB.
